git_url("https://github.com/rentachef/rentachef-react-native.git")

storage_mode("git")

type("appstore") # The default type, can be: appstore, adhoc, enterprise or development
cert_path("/Users/distiller/rentachef-react-native/.circleci/certs/distribution.cer")
cert_key_path("/Users/distiller/rentachef-react-native/.circleci/certificate.p12")
git_branch("circleci_with_code_signing_step")

# app_identifier(["com.raclabs.rentachef"])
# username("junaid.jp@gmail.com") # Your Apple Developer Portal username

# For all available options run `fastlane match --help`
# Remove the # in the beginning of the line to enable the other options

# The docs are available on https://docs.fastlane.tools/actions/match

